/* Kept provided starter code as is and created a header file with which to import into main */

//preprocessor include statements
#include "Python.h"
#include <iostream>
#include <Windows.h>
#include <cmath>
#include <string>
#include <cstring>  //added cstring for strcpy

//function declarations
void CallProcedure(std::string pName);
int callIntFunc(std::string proc, std::string param);
int callIntFunc(std::string proc, int param);


